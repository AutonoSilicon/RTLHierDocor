# WebUI dependencies for RTLHierDocor
# Install with: pip install -r requirements-webui.txt

# Backend
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=11.0

# Frontend build tools are managed via npm in webui/package.json
