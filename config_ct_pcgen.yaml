# RTLHierDocor Test Configuration for ct_ifu_pcgen
#
# Usage: python3 -m cli docor -c config_ct_pcgen.yaml

# ── Design source ──────────────────────────────────────────────
design:
  filelist: "$CODE_BASE_PATH/gen_rtl/filelists/C910_asic_rtl.fl"
  top_module: "ct_ifu_pcgen"

# ── Output settings ───────────────────────────────────────────
# Output dir uses default "./rtl_docs" if not specified
# output:
#   dir: "./rtl_docs_ct_pcgen"
#   cache_dir: ".rtl_cache"

# ── Schematic simplification ──────────────────────────────────
simplify:
  enabled: true
  strategy: "proc_group"
  # remove_signals:
  #   - "cpurst_b"
  #   - "*rst_b"
  #   - "pad_yy_icg_scan_en"
  #   - "*scan_en*"

# ── General ───────────────────────────────────────────────────
verbose: true

# ── Agent Docor ────────────────────────────────────────────────
agent:
  backend: "openai"
  model: "qwen3.5-plus"
  base_url: "https://coding.dashscope.aliyuncs.com/v1"
  api_key: "sk-sp-0b4bfeb434d946bda2d005ac9c85026e"
  composer_model: "deepseek-v3.2"
  composer_base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  composer_api_key: "sk-22730e3d9366402489918930ed806b32"
  thinking: true
  skip_modules:
    - "ct_had*"
  max_source_lines: 5000
  resume: true
  code_base_path: "$CODE_BASE_PATH"
  max_modules: 0
