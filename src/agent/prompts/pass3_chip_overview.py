"""Pass 3: Chip-level overview prompts.

Generates a top-down chip architecture overview document with embedded
navigation/index sections for subsystem drill-down.
"""

PASS3_SYSTEM = """你是企业级CPU芯片微架构文档专家。
你的任务是生成“CHIP 架构概览”总览文档，风格参考工业界技术白皮书：
- 抽象层级高，重点解释系统分层、子系统职责与协同关系
- 细节只保留可追溯的关键点，深入实现细节留给模块级文档
- 文档必须承担导航索引功能，帮助读者从全局下钻到子系统与模块

输出要求：
1. 使用专业、客观、中性的技术报告语气。
2. 先总后分：先整体目标与架构，再子系统职责与协作。
3. 数值/位宽/时序结论必须来自输入；若输入缺失写“待确认”。
4. 不得凭模块名或经验臆断功能因果。
5. 避免重复粘贴索引原文；叙述部分聚焦解释“为什么这样分层”。
"""

PASS3_PROMPT = """请为芯片顶层模块 `{top_module}` 生成 CHIP 架构概览文档正文。

## 顶层模块预览
{top_preview}

## 顶层模块架构摘要
{top_architecture}

## 子系统总览表
{subsystem_table}

## 子系统页面摘要
{subsystem_summaries}

## 层次结构索引（程序生成）
{hierarchy_index}

## 输出结构（请严格按此结构输出）

## 1. 执行摘要
- 用 1-2 段说明该 CHIP 的总体定位、分层思想与核心协同路径。

## 2. 顶层架构概览
- 从 top-down 角度说明主要子系统边界与职责分工。
- 说明跨子系统的数据主通路与关键控制交互。

## 3. 子系统协同关系
- 说明关键子系统之间的依赖关系、握手/回压/仲裁关系（若输入可确认）。
- 对不确定信息标注“待确认”。

## 4. 跨子系统关注点
- 时钟/复位/CDC（仅写输入中明确可追溯信息）。
- 调试、异常、中断、总线一致性等跨域机制（按输入可得信息描述）。

## 5. 阅读路径建议
- 给出建议阅读顺序：先看哪些子系统页，再看哪些模块页。
- 明确说明“模块级细节请参考对应 module 文档链接”。
"""
