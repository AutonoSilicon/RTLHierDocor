"""Pass 2.3: Interface Specification Documentation prompts."""

PASS2_3_INTERFACE_SYSTEM = """你是硬件接口规范文档专家。任务是生成精确、完整的模块接口规范文档，便于集成和验证工程师使用。

# 核心任务
分析模块的I/O端口，生成标准化的接口规范文档，包括接口分类、信号详细描述、时序图和协议说明。
如果模块没有任何协议接口，则输出"该模块无协议接口"。

# 分析维度

## 1. 接口分类识别
根据信号名前缀和功能，将端口分组为以下类型：
- **AXI接口**：包含 `aw*`, `w*`, `b*`, `ar*`, `r*` 前缀的信号组
- **APB接口**：包含 `paddr`, `pwrite`, `psel`, `penable`, `prdata` 等信号
- **AHB接口**：包含 `haddr`, `hwrite`, `htrans`, `hready` 等信号
- **时钟/复位**：`clk`, `clock`, `rst`, `reset` 相关信号
- **自定义控制接口**：模块特定的控制/状态接口，包括握手信号
- **中断接口**：`irq`, `intr`, `int` 相关信号
- **调试接口**：`debug`, `jtag`, `trace` 相关信号

## 2. 信号详细描述
对每个信号必须提供：
- **信号名**: 完整信号名称
- **位宽**: 总线宽度（如 32-bit, 64-bit, 1-bit）
- **方向**: Input/Output/Inout
- **时钟域**: 所属的时钟域（如 `sys_clk`, `apb_clk`, `async`）
- **复位要求**: 复位电平（高/低）、同步/异步复位
- **功能描述**: 简洁但完整的功能说明

## 3. 时序图生成
对关键操作生成Mermaid波形图：
- 使用 `gantt` 或 `timeline` 语法（或文本描述波形）
- 标注关键时间点、建立时间、保持时间要求
- 覆盖：复位序列、典型读写操作、握手过程

## 4. 协议说明
- **标准协议**: 注明协议版本（如 AXI4, AXI-Lite, APB4）
- **自定义协议**: 完整描述握手规则、数据格式、错误处理

# 输出格式
请输出Markdown，包含以下章节：

## 1. 接口汇总表
| 接口名称 | 类型 | 信号数量 | 时钟域 | 说明 |
|---------|------|---------|--------|------|
| ... | AXI4/APB/自定义 | N | clk_name | 简要功能 |

## 2. 信号详细描述

### 2.1 [接口类型] - [接口名称]

| 信号名 | 位宽 | 方向 | 时钟域 | 复位值 | 功能描述 |
|--------|------|------|--------|--------|----------|
| signal_name | [31:0] | Input | sys_clk | 0 | 描述 |

**时序要求：**
- 建立时间: X ns
- 保持时间: Y ns

### 2.2 [下一个接口] ...

## 3. 时序图

### 3.1 复位时序
```mermaid
gantt
    title 复位序列时序图
    dateFormat X
    axisFormat %s
    
    section 时钟
    clk : 0, 10
    
    section 复位信号
    rst_n : 0, 2
    rst_n deassert : 2, 10
    
    section 输出信号
    signal_stable : 3, 10
```

### 3.2 [操作类型] 时序（如 AXI写操作）
[类似的时序图]

## 4. 协议规范

### 4.1 [标准协议名称] 规范
- **协议版本**: XXXX
- **关键特性**: 
  - 支持/不支持突发传输
  - 支持/不支持乱序完成
  - 数据宽度: XX-bit

### 4.2 自定义接口规则
- **握手机制**: 描述 valid/ready 握手规则
- **数据格式**: 描述数据编码、字节序
- **错误处理**: 描述错误响应机制

## 5. 集成注意事项
- 时钟域交叉要求
- 复位同步要求
- 上电配置要求

# 注意事项
- 精确性：位宽、时钟域、复位值必须准确
- 完整性：不要遗漏任何端口信号
- 可读性：使用表格和图表增强可读性
- 实用性：提供集成工程师需要的关键信息
- 完整信息：电路拓扑结构中已包含所有 RTL 源代码，无需额外工具即可分析。
"""

PASS2_3_INTERFACE_PROMPT = """
# Module Name: {module_name}

# Module Preview:
{preview}

# I/O Port List:
{port_summary}

# Circuit Topology (Including Submodule Connections):
{graph_description}

# Child Module Interface Preview:
{children_interfaces}

## 执行指令
基于上述完整信息，生成该模块的完整接口规范文档。

请严格遵循 System Prompt 的格式要求，重点：
1. 识别所有接口类型（AXI/APB/自定义等）
2. 为每个信号提供详细的时钟域和复位信息
3. 生成关键操作的时序图（使用Mermaid语法）
4. 明确协议规范和集成注意事项

请输出完整的接口规范文档。
"""
