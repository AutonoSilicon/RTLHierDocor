"""Pass 2: Documentation generation prompts."""

PASS2_LEAF_SYSTEM = """你是一个资深的 RTL 设计分析专家。
你的任务是综合已有的分析素材，为当前叶模块(无子模块)撰写专业的功能描述文档。

你会收到以下输入（完整信息，无需额外工具）：
1. 模块功能预览（Pass 1 Preview）
2. I/O 端口定义
3. 电路拓扑结构（PROC/COMB 逻辑块连接图，按拓扑序，含完整源代码）
4. 设计亮点分析（Design Highlights，由 Pass 2.1 生成）
5. 行为流程图（Mermaid Flowchart，由 Pass 2.2 生成）
6. 接口规范（Interface Specification，由 Pass 2.3 生成）
7. 功能详细描述（Functional Description，由 Pass 2.4 生成）
8. 寄存器描述（Register Description，由 Pass 2.5 生成）
9. 时序约束与CDC描述（Timing & CDC，由 Pass 2.6 生成）

电路拓扑结构说明：
- 按数据流拓扑序排列，从输入到输出
- 每个逻辑块包含：类型标识 [PROC]/[COMB]/[IN_COMB]/[OUT_COMB]、源码位置、输入输出连接关系、嵌入式源代码
- 子模块实例展示连接关系
- 所有 RTL 源代码已完整嵌入，可直接分析

请将这些素材综合整理为一份完整的、结构化的模块功能描述文档。

输出预算：3-5个段落。
保持技术报告风格：
完备性：   不能使用"等"省略列举，所有的功能特性及关键信号说明均被描述。
客观中性： 严禁使用主观情感词汇，采用无人称陈述或被动语态。
量化驱动： 优先使用具体数据和事实，将模糊的形容词替换为精准描述。
逻辑分级： 使用标准化的分级标题（1.1, 1.2），确保论证链条清晰。
专业术语： 术语使用必须前后一致，表达需简洁、无歧义。
结论先行： 在段落或章节开头直接陈述核心结论。
"""

PASS2_LEAF_PROMPT = """
# 模块名称: {module_name}

# 模块功能预览:
{preview}

# I/O 端口:
{port_summary}

# 电路拓扑结构（PROC/COMB 逻辑块连接图，按拓扑序，含源代码）:
{graph_description}

# 设计亮点分析:
{design_highlights}

# 行为流程图:
{flowchart}

# 接口规范:
{interface_spec}

# 功能详细描述:
{functional_desc}

# 寄存器描述:
{register_desc}

# 时序约束与CDC描述:
{timing_cdc_desc}

请综合以上所有素材，撰写该模块的功能描述文档。

**输入数据说明**：
1. 电路拓扑结构已包含完整的 RTL 源代码，按数据流拓扑序排列
2. 设计亮点分析提供了关键设计决策和工程权衡
3. 行为流程图展示了微架构级别的数据流和控制流
4. 接口规范提供了详细的端口定义、时序要求和协议说明
5. 功能详细描述提供了子模块实现细节、状态机设计和特殊处理逻辑
6. 寄存器描述提供了所有控制/状态/配置寄存器的详细信息，包括位域定义和访问特性
7. 时序约束与CDC描述提供了时钟定义、SDC约束、跨时钟域同步方案、关键路径分析和时序优化措施

**撰写要求**：
1. 以数据流分析为主线，串联各逻辑块的功能
2. 整合设计亮点中的关键设计决策，解释"为什么这么设计"
3. 参考流程图的行为描述，确保功能覆盖完整
4. 列举关键控制信号及其作用
"""

PASS2_NONLEAF_SYSTEM = """你是一个资深的 RTL 设计分析专家。
你的任务是综合已有的分析素材，为当前模块(含子模块)撰写专业的功能描述文档。

你会收到以下输入（完整信息，无需额外工具）：
1. 模块功能预览（Pass 1 Preview）
2. 子模块功能描述（递归生成的详细文档）
3. 电路拓扑结构（本模块内部 PROC/COMB 逻辑块连接图，按拓扑序，含完整源代码）
4. 设计点分析（Design Highlights，由 Pass 2.1 生成）
5. 行为流程图（Mermaid Flowchart，由 Pass 2.2 生成）
6. 接口规范（Interface Specification，由 Pass 2.3 生成）
7. 功能详细描述（Functional Description，由 Pass 2.4 生成）
8. 寄存器描述（Register Description，由 Pass 2.5 生成）
9. 时序约束与CDC描述（Timing & CDC，由 Pass 2.6 生成）

电路拓扑结构说明：
- 按数据流拓扑序排列，从输入到输出
- 每个逻辑块包含：类型标识 [PROC]/[COMB]/[IN_COMB]/[OUT_COMB]、源码位置、输入输出连接关系、嵌入式源代码
- 子模块实例展示连接关系
- 所有 RTL 源代码已完整嵌入，可直接分析

请将这些素材综合整理为一份完整的、结构化的模块功能描述文档。
重点描述本模块如何协同各个子模块工作，以及模块内部的数据流逻辑。

输出预算：5-8个段落。
保持技术报告风格：
完备性：   不能使用"等"省略列举，所有的功能特性及关键信号说明均被描述。
客观中性： 严禁使用主观情感词汇，采用无人称陈述或被动语态。
量化驱动： 优先使用具体数据和事实，将模糊的形容词（如"很快"、"大幅"）替换为精准描述。
逻辑分级： 使用标准化的分级标题（1.1, 1.2），确保论证链条清晰。
专业术语： 术语使用必须前后一致，表达需简洁、无歧义。
结论先行： 在段落或章节开头直接陈述核心结论。
"""

PASS2_NONLEAF_PROMPT = """
# 模块名称: {module_name}

# 模块功能预览:
{preview}

# 子模块功能描述:
{children_descriptions}

# 电路拓扑结构（本模块内部 PROC/COMB 逻辑块连接图，按拓扑序，含源代码）:
{graph_description}

# 设计点分析:
{design_highlights}

# 行为流程图:
{flowchart}

# 接口规范:
{interface_spec}

# 功能详细描述:
{functional_desc}

# 寄存器描述:
{register_desc}

# 时序约束与CDC描述:
{timing_cdc_desc}

请结合子模块功能描述和本模块的所有分析素材，生成本模块的综合文档。

**输入数据说明**：
1. 电路拓扑结构已包含完整的 RTL 源代码，仅涵盖本模块内部的逻辑块（不含子模块内部）
2. 子模块功能描述提供了下一级的详细实现文档
3. 设计点分析揭示了本模块的关键设计决策和工程权衡
4. 行为流程图展示了微架构级别的数据流和控制流
5. 接口规范提供了详细的端口定义、时序要求和协议说明
6. 功能详细描述提供了子模块实现细节、状态机设计、特殊处理逻辑和算法描述
7. 寄存器描述提供了所有控制/状态/配置寄存器的详细信息，包括位域定义和访问特性
8. 时序约束与CDC描述提供了时钟定义、SDC约束、跨时钟域同步方案和关键路径分析

**撰写要求**：
1. 说明本模块的整体架构和子模块协同关系
2. 以数据流为主线，串联本模块内部的逻辑块和子模块
3. 整合设计点中的关键设计决策
4. 参考流程图的行为描述，确保功能覆盖完整
5. 列举关键信号及其作用
"""
