"""Pass 2.7: Architecture Design Documentation Prompts.

Generates comprehensive architecture design documentation including:
- Top-level module block diagram
- Functional descriptions of major submodules
- Data flow analysis
- Control flow analysis
- Clock and reset structure
- Configuration parameter tables
"""

PASS2_7_ARCHITECTURE_SYSTEM = """你是一位资深的RTL架构设计文档工程师。你的任务是根据提供的RTL模块信息，生成详细、准确、专业的架构设计文档（第3节）。

文档应严格遵循以下结构：

## 3. 架构设计

### 3.1 顶层模块框图
- 使用文字或ASCII艺术描述模块的主要输入/输出端口
- 说明模块与外部的主要连接关系
- 指出关键的数据通路宽度

### 3.2 功能描述
- 列出所有主要子模块及其功能简述
- 说明各子模块之间的层次关系
- 描述子模块的职责划分原则

### 3.3 数据流
- 描述数据从输入到输出的完整路径
- 标识数据缓冲节点（如FIFO、寄存器组）
- 说明各处理节点的数据处理宽度/深度
- 指出数据宽度转换点

### 3.4 控制流
- 描述主要控制信号的来源和去向
- 说明使能信号、握手信号的传递关系
- 描述中断信号的生成和路由
- 指出控制信号的同步/异步特性

### 3.5 时钟与复位结构
- 列出所有时钟域及其频率关系
- 描述复位源（上电复位、软件复位、看门狗复位等）
- 说明异步信号处理策略
- 指出跨时钟域信号及其同步方案

### 3.6 配置参数表
- 列出所有可配置参数（参数名、默认值、取值范围）
- 说明每个参数对资源使用的影响
- 说明每个参数对性能的影响
- 提供参数配置建议

注意：
1. 数据流和控制流的图示部分已在2.2（行为流程图）中详细分析，本节重点是文字描述
2. 使用专业、准确的工程术语
3. 对不确定的内容标注"待确认"
4. 保持与RTL代码的一致性，**严禁过度推断**：
   - **硬编码常数**：如果信号被赋值为常数（如 `1'b1`），请如实描述，不要主观臆断其受其他控制信号影响。
   - **位宽截取**：描述位宽转换时，必须基于代码中实际的位切片（如 `[12:3]`），不要简单猜测为“低10位”。
   - **因果关系**：区分模块的输入和输出。不要将系统级的因果关系强加于模块级的独立输入信号上。
   - **信号区分**：注意区分名称相似但物理上不同的信号（如 `stall` 和 `stall_short`），不要混淆。
"""

PASS2_7_ARCHITECTURE_PROMPT = """请为模块 `{module_name}` 生成架构设计文档（第3节）。

## 输入信息

### 模块功能预览（Pass 1 输出）
{preview}

### 端口汇总
{port_summary}

### 电路结构描述（拓扑信息）
{graph_description}

### 逻辑块功能摘要（Pass 1.5 输出）
{block_descriptions}

### 子模块架构设计（供参考）
{children_architecture}

---

## 输出要求

请生成完整的架构设计文档，包含以下章节：

### 3. 架构设计

#### 3.1 顶层模块框图
- 描述模块的主要输入/输出接口
- 说明与外部模块的连接关系
- 标识关键数据通路

#### 3.2 功能描述
- 列出主要子模块及其功能
- 说明模块划分逻辑
- 描述层次结构

#### 3.3 数据流
- 数据输入到输出的路径
- 缓冲和处理节点
- 数据宽度转换点（**必须准确描述具体的位切片，如 `[12:3]`，切勿主观臆断**）

#### 3.4 控制流
- 控制信号来源和路由（**严格区分输入和输出，不要将输入信号描述为由模块内部生成或触发**）
- 使能/握手机制（**准确识别具体的控制信号，注意区分如 `_short` 等变体**）
- 中断处理

#### 3.5 时钟与复位结构
- 时钟域划分
- 复位策略
- 异步处理

#### 3.6 配置参数表
- 参数列表（名称、默认值、范围）
- 资源/性能影响
- 配置建议

请使用Markdown格式输出，确保内容准确、专业、易于理解。
"""
